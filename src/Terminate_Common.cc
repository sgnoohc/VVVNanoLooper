#include "Terminate_Common.h"

void Terminate_Common()
{
    //==============================================
    // Terminate_Common:
    // This function gets called after the event looping.
    // This is where one does stuff that needs to happen after the event looping for category Common.
    //==============================================

    // To place eventlist writing or something that needs to happen post loop.
    // TODO: implement event list writing
}
