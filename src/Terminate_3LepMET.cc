#include "Terminate_3LepMET.h"

void Terminate_3LepMET()
{
    //==============================================
    // Terminate_3LepMET:
    // This function gets called after the event looping.
    // This is where one does stuff that needs to happen after the event looping for category 3LepMET.
    //==============================================

    // To place eventlist writing or something that needs to happen post loop.
    // TODO: implement event list writing
}
